% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/databases.R
\name{imf_cofer}
\alias{imf_cofer}
\title{Currency composition of official foreign exchange reserves (COFER)}
\usage{
imf_cofer(
  currency = "USD",
  measure = c("share", "value"),
  group = "G001",
  freq = "Q",
  start = NULL,
  end = NULL
)
}
\arguments{
\item{currency}{Reserve currency code: \code{"USD"}, \code{"EUR"}, \code{"JPY"}, \code{"GBP"},
\code{"CNY"} (or \code{"T"} for the total of allocated reserves).}

\item{measure}{\code{"share"} (percent of allocated reserves) or \code{"value"}
(nominal US dollars).}

\item{group}{Aggregate code; \code{"G001"} (the World) by default.}

\item{freq}{Frequency code (\code{"Q"} or \code{"A"}).}

\item{start, end}{Optional first and last year to keep.}
}
\value{
An object of class \code{imf_tbl} (a data frame).
}
\description{
The share, or nominal value, of a reserve currency in allocated world foreign
exchange reserves, from the IMF \code{COFER} data flow. COFER is published only
for world and country-group aggregates, never individual economies.
}
\examples{
\donttest{
# US dollar share of world allocated reserves
imf_cofer(currency = "USD", measure = "share")
}
}
\seealso{
\code{\link[=imf_data]{imf_data()}}
}
