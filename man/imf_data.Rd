% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/structure.R
\name{imf_data}
\alias{imf_data}
\title{Fetch data from any IMF data flow}
\usage{
imf_data(
  dataflow,
  key = NULL,
  agency = NULL,
  version = "+",
  start = NULL,
  end = NULL
)
}
\arguments{
\item{dataflow}{Data flow id, for example \code{"WEO"} or \code{"CPI"}.}

\item{key}{Either a ready-made key string (for example \code{"GBR.NGDP_RPCH.A"}),
or a list of values per dimension in order (for example
\code{list(c("GBR", "USA"), "NGDP_RPCH", "A")}), or \code{NULL} for all series. An
empty element is a wildcard for that dimension.}

\item{agency}{Owning agency. If \code{NULL} it is looked up from the catalogue.}

\item{version}{Data flow version; \code{"+"} (the default) requests the latest.}

\item{start, end}{Optional first and last year to keep.}
}
\value{
An object of class \code{imf_tbl} (a data frame).
}
\description{
The general-purpose accessor behind the database-specific wrappers. Supply a
data flow id and a key. Use \code{\link[=imf_dimensions]{imf_dimensions()}} to discover the dimension
order and \code{\link[=imf_codelist]{imf_codelist()}} to discover valid codes.
}
\examples{
\donttest{
imf_data("WEO", "GBR.NGDP_RPCH.A", start = 2015, end = 2024)
}
}
