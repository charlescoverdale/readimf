% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ifs.R
\name{imf_ifs}
\alias{imf_ifs}
\title{Fetch a legacy IFS series from its new home}
\usage{
imf_ifs(indicator, country = NULL, freq = NULL, start = NULL, end = NULL)
}
\arguments{
\item{indicator}{A legacy IFS indicator code, for example \code{"PCPI_IX"}
(consumer price index) or \code{"ENDA_XDC_USD_RATE"} (exchange rate per USD).}

\item{country}{Character vector of ISO 3-letter country codes, for example
\code{c("GBR", "USA")}. Required: the underlying data flows do not serve
wildcard country queries without an API key.}

\item{freq}{Frequency code (\code{"A"}, \code{"Q"} or \code{"M"}). Defaults to the series'
usual frequency.}

\item{start, end}{Optional first and last year to keep.}
}
\value{
An object of class \code{imf_tbl} (a data frame). The originating IFS code
is retained in the \code{imf_ifs_code} attribute.
}
\description{
International Financial Statistics (IFS) was retired when the IMF moved to
the 'SDMX' 3.0 portal. Its content was split across several thematic data
flows (consumer prices, exchange rates, monetary statistics and others) with
no official code crosswalk, which broke every workflow built on the old IFS
series codes. \code{imf_ifs()} accepts a legacy IFS indicator code and routes the
request to the correct new data flow and key.

Use \code{\link[=imf_ifs_map]{imf_ifs_map()}} to see which legacy series are supported and where each
one now lives. Country coverage in the new data flows varies, especially for
interest rates; if a country returns nothing, try another.
}
\examples{
\donttest{
# UK consumer price index, the series formerly known as IFS PCPI_IX
imf_ifs("PCPI_IX", country = "GBR", start = 2015)
}
}
\seealso{
\code{\link[=imf_ifs_map]{imf_ifs_map()}}, \code{\link[=imf_data]{imf_data()}}
}
